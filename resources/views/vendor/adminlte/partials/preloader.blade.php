{{-- Preloader Tanpa Animasi --}}
<div class="preloader flex-column justify-content-center align-items-center">
    <img class="" src="{{ asset(config('adminlte.preloader.img.path')) }}"
         alt="{{ config('adminlte.preloader.img.alt') }}"
         height="{{ config('adminlte.preloader.img.height', 60) }}"
         width="{{ config('adminlte.preloader.img.width', 60) }}">
</div>
